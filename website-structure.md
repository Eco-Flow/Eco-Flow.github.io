### Pipelines tab

This tab depends is built using:
- `nav-items.html`.
- `main.scss`. Drop down section.
- `_pipelines_2` folder. Every page inside has a header with the order of appearance.
- `_config.yml`. The drop down is activated in the collections section.
- `header.html`. I think the nav class section also helps, although not entirely sure.